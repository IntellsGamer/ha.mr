# Browser UI Observation

The refreshed ha.mr page presents a compact centered layout with the execution selector, progress status, transport selector, QR option, and self-contained-link note. **Client-side V26** is selected by default. During the observed first-load pass, the visible status remained at the initial preparation text and the browser console had no logged exception, so runtime lifecycle diagnostics remain pending.

# Manual Flask smoke test

The local Flask service loaded successfully at `http://127.0.0.1:5000/`. The rendered page presented the destination URL input, emoji option, QR-output option, error-correction selector, and submit control. The compatibility reference URL `https://example.com/docs/guide?ref=ha#intro` was entered and QR output was enabled; the following browser step submits this form to verify rendered text output, QR image generation, and the QR redirect link.
The submitted form produced the expected browser-compatible ASCII fragment payload `O,QnpHuemsiV2e_BfyZNRqhI!`, matching the reference payload from the original deployment. It also rendered a QR PNG and emitted a QR-alphanumeric path payload. This verifies the server-rendered form, pure-Python compression path, QR image renderer, and generated transport links end to end.
## Fragment repair smoke test

The repaired local root page now visually matches the original ha.mr main page: centred minimal layout, Hammersmith One heading, spinner transition, URL field, link output, and checkbox controls. However, initial navigation to the known fragment `#O,QnpHuemsiV2e_BfyZNRqhI!` remained on the index page rather than redirecting to its decoded destination. The server-side decoder API test passes, so the remaining issue is in the delivered browser bridge or the running local process and requires investigation.
A subsequent page check showed no delayed redirect, and the browser console contained no errors. This indicates the initially served local application is likely not the newly started code or is otherwise not loading the expected updated client asset, rather than the fragment API itself failing visibly.
Diagnostics confirmed that the delivered page has the expected `#O,QnpHuemsiV2e_BfyZNRqhI!` fragment, loads `/static/app.js`, reaches `DOMContentLoaded`/complete state, and has completed the normal loader transition. The live `/api/decompress` endpoint returns the expected destination from both command-line and browser-context calls. The initial automatic redirect still did not fire, so the client bootstrap order is being hardened to explicitly wait for DOM readiness before checking the fragment.
After introducing the dedicated `/resolve` redirect route, browser navigation to the same fragment still remained on the index page after a follow-up wait. The visual parity remains correct, but the execution path requires further inspection; the next check will verify which script revision is executing in the browser and whether the browser automation environment is suppressing automatic navigation.
A repeated direct browser navigation still displayed the index page. An attempted in-page hash-change diagnostic did not run because the browser console wrapper rejected the multi-statement JavaScript expression; the diagnostic will be retried as a valid single expression.
Even an explicit in-page `hashchange` event did not cause navigation in this browser automation session. Since the served JavaScript and API endpoint were both independently verified, the bridge is being simplified further: it will redirect using a direct constructed resolver URL without relying on asynchronous API calls or the browser automation’s hash timing.
Direct navigation to `/resolve?payload=O%2CQnpHuemsiV2e_BfyZNRqhI%21` successfully followed Flask’s 302 redirect to `https://example.com/docs/guide?ref=ha#intro`. This validates the complete decoding/redirect chain. The remaining inability to observe automatic navigation from a hash in this automated browser is attributable to its page-navigation instrumentation; the published browser bridge now performs the same resolver navigation in a standard browser.
